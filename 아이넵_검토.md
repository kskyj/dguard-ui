https://121.130.177.24:29443/app
계정 정보는 inebsoft / Ineb!@#$5 (첫문자 I 는 대문자입니다.)
강재홍 드림.

-----
# 점검대상
- 검색수 컬럼 누락 : DB의 검색 돌린 개수
- 표 높이 조금 늘려주세요.(기존 mockup 참고)
- 연결상태 sort 추가
- 연결상태 필터에 추가

# 점검대상 상세
- 검사규칙 등록 > `업데이트된 로우만 검출` --> `업데이트된 ROW만 검출` 문구 변경
- 점검자는 이름(ID) 형태로 나와야함 김성진(5492438)
- 필터 프로필 추가가 활성화 되지 않음
- 필터 프로필 소급적용 하면 activating 나오고나서 상태가 안바뀌고, 검출된 데이터가 보이지 않음
- 필터프로필의 내용을 볼 수 없음
- 검사범위 설정에서 일괄 삭제 기능 필요


# 검출목록
- 20개만 있는 상태에서 다음 내용이 있을거라고 시각적으로 잘 안보입니다. 기본을 100개로 해주세요.
- 제외는 기본으로 보이지 말아야함
- 오른쪽 검출내용 표는 height 를 많이 줄여서 하단에 검출값이 있는지 인지가 되야합니다.
- 검출타입, 담당자, 상태 표 상단 누르면 Sort 필요
- 검출된 개인정보가 1건일 경우 아래 검출내용 표 는 안나오게 해주세요.
- 이행점검이 검출된 개인정보가 동일한개 여러건이면 해당 유니크키가 전부 이행점검이 되나요? 지금 상태의 UI로는 그렇게 느껴 집니다.
- 검색에 검출ID 추가
- 검출목록 표의 검출경로 등의 컬럼 길이 조절이 마우스로 가능해야함
- 검출 의견은 항상 마지막 history의 의견이 보여야함
- 소급적용으로 개수가 사라져서 건수가 0이되어도, 검출목록에서는 보이고 있음 --> 검출건수가 0이면 보이지 않아야함
- 검출목록 > 내보내기 하면 엑셀파일에서 상태가 영어로 나옴 --> 한글로 변경
- 엑셀파일에서 링크는 microsoft-edge: 스킴을 추가
- row를 선택하면 해당 row 가 선택된것을 인지 할수 없음
 ㅇ 체크박스만 표시하고, 선택한 ROW만 배경색이 변경되어야함 mockup 참고
 ㅇ 선택한 row의 검출경로, 검출타입, 건수 가 상태 위에 보여야함 mockup 참고
- 조치계획서 작성 조치필요만 선택되었을때 버튼이 활성화 되는데, 어떤 조건에 조치계획서 작성이 나오는지 알기 어려움. 체크가 1개라도 되면 조치필요버튼이 활성화 되고, 상태가 조치필요가 아닌것이 1개라도 있을 경우 팝업으로 `조치필요 상태만 조치계획 작성이 가능합니다` 나올것
- 사후필터가 완료 안되면 검출내용이 나오지 않는 현상


# 게시판
- 커뮤니티 > 게시판 으로 변경 요청
- 공지사항 과 QnA 가 화면에 양쪽에 나오는데, 서브 메뉴 2개로 분류

# 제외신청목록
- 신청목록/처리결과 에서 탐지ID 클릭시 검출 목록으로 이동 필요
- 제외승인/거절 시 의견을 적으면 검출목록의 해당 검출ID의 의견은 마지막 의견으로 보여야함

# 조치계획관리
- 조치계획서에 첨부파일에 DRM걸린 문서 올리면 업로드 되지 않음

# 예외 필터 관리
- 필터테스트 동작 안함
- Exclude locations by.. 와 같은 영어는 제거(수정/추가시 나옴)

# 테이블 점검 성공/실패 내역
- DB서버 전체를 돌렸을떄 특정 테이블을 점검했는지 확인이 가능해야함
- 테이블의 성공/실패 여부 가 보여야함
- 컬럼1개를 점검해도 테이블 1개로 count
- 점검이력 표에 검색 테이블 수 추가
- 점검이력 표에 실패 테이블 수 추가
- 실패 테이블 개수를 누르면 스키마.테이블 과 실패메시지 보여야함

# 점검이력
- 점검자는 이름(ID) 형태로 나와야함 김성진(5492438)
- 스케줄 선택 -> 오른쪽 사이드바 수정버튼 이 없음 : 기존 스케줄을 수정해서 진행이 필요함
- 점검이력 표에 검색 테이블 수 추가
- 점검이력 표에 실패 테이블 수 추가
- 실패 테이블 개수를 누르면 스키마.테이블 과 실패메시지 보여야함

# 기타
- 표 헤더 글자 색상이 회색이 잘 안보임(기존 mockup 참고)
- 일반사용자 로그인시 검출목록으로 이동안되고 에러발생함
- 컬럼단위 점검하는 기능 필요
